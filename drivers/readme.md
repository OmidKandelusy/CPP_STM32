# NOTES
This directory contains the driver implementations ans APIs where all is written in C
This repository should not be used directly in application development, except for testing
and debugging purposed. Otherwise, the application or subsystem level codes should use the
`driver_classes` directory which provide scalable and modualar interfaces for hardware access
via C++ classes and interfaces.